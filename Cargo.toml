[package]
name = "greentunnelRS"
version = "0.1.0"
edition = "2021"
rust-version = "1.80"
authors = ["Ali Kemal Öcalan <alikemalocalan@gmail.com>"]
description = "Ultra-fast, lightweight anti-censorship DPI bypass HTTP/HTTPS proxy written in Rust for desktop, Linux servers, macOS, and OpenWrt routers."
readme = "README.md"
license = "MIT OR Apache-2.0"
repository = "https://github.com/alikemalocalan/greentunnelRS"
keywords = ["anti-censorship", "dpi-bypass", "proxy", "doh", "openwrt"]
categories = ["network-programming", "command-line-utilities"]

[[bin]]
name = "greentunnelRS"
path = "src/main.rs"

[dependencies]
tokio = { version = "1.53.1", features = ["rt-multi-thread", "net", "io-util", "time", "macros", "sync"] }
rustls = { version = "0.23.43", features = ["ring", "std"], default-features = false }
tokio-rustls = { version = "0.26.4", features = ["ring"], default-features = false }
tracing = { version = "0.1.44", features = ["std"], default-features = false }
rand = "0.10.2"
anyhow = "1.0.104"
socket2 = "0.6.5"
webpki-roots = "1.0.9"

[profile.dev]
opt-level = 0
debug = true

[profile.test]
opt-level = 1

[profile.release]
opt-level = "s"
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true

[lints.rust]
unsafe_code = "forbid"
unused_must_use = "warn"

[lints.clippy]
all = "warn"

